[phases.setup]
# nixpkgs uses attributes like nodejs_18 / nodejs_20 / nodejs_22 (not nodejs-22_x)
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm ci",
  "npx prisma generate"
]

[phases.build]
cmds = [
  "npm run build"
]

[start]
cmd = "npm run start:prod"
